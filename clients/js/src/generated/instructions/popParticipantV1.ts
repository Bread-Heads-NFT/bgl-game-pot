/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type PopParticipantV1InstructionAccounts = {
  /** The address of the game pot */
  pot: PublicKey | Pda;
  /** The authority of the game pot */
  gameAuthority: Signer;
  /** The participant to remove from the allowlist */
  participant: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type PopParticipantV1InstructionData = { discriminator: number };

export type PopParticipantV1InstructionDataArgs = {};

export function getPopParticipantV1InstructionDataSerializer(): Serializer<
  PopParticipantV1InstructionDataArgs,
  PopParticipantV1InstructionData
> {
  return mapSerializer<
    PopParticipantV1InstructionDataArgs,
    any,
    PopParticipantV1InstructionData
  >(
    struct<PopParticipantV1InstructionData>([['discriminator', u8()]], {
      description: 'PopParticipantV1InstructionData',
    }),
    (value) => ({ ...value, discriminator: 2 })
  ) as Serializer<
    PopParticipantV1InstructionDataArgs,
    PopParticipantV1InstructionData
  >;
}

// Instruction.
export function popParticipantV1(
  context: Pick<Context, 'programs'>,
  input: PopParticipantV1InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglGamePot',
    'BHPoT3HQSWiPD8AczqvCbudyicPmE8RN8bpcyZLzMGmb'
  );

  // Accounts.
  const resolvedAccounts = {
    pot: { index: 0, isWritable: true as boolean, value: input.pot ?? null },
    gameAuthority: {
      index: 1,
      isWritable: true as boolean,
      value: input.gameAuthority ?? null,
    },
    participant: {
      index: 2,
      isWritable: false as boolean,
      value: input.participant ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getPopParticipantV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
