/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { findAssociatedTokenPda } from '@metaplex-foundation/mpl-toolbox';
import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findGamePotPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type PayOutSplTokenV1InstructionAccounts = {
  /** The address of the game pot */
  pot?: PublicKey | Pda;
  /** The authority of the game pot */
  gameAuthority: Signer;
  /** The account receiving the payout */
  winner: PublicKey | Pda;
  /** The address of the token mint */
  tokenMint: PublicKey | Pda;
  /** The source token account */
  potTokenAccount?: PublicKey | Pda;
  /** The destination token account */
  winnerTokenAccount?: PublicKey | Pda;
  /** The token account of the game authority */
  gameAuthorityTokenAccount?: PublicKey | Pda;
  /** The token program */
  tokenProgram?: PublicKey | Pda;
  /** The associated token program */
  associatedTokenProgram?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type PayOutSplTokenV1InstructionData = { discriminator: number };

export type PayOutSplTokenV1InstructionDataArgs = {};

export function getPayOutSplTokenV1InstructionDataSerializer(): Serializer<
  PayOutSplTokenV1InstructionDataArgs,
  PayOutSplTokenV1InstructionData
> {
  return mapSerializer<
    PayOutSplTokenV1InstructionDataArgs,
    any,
    PayOutSplTokenV1InstructionData
  >(
    struct<PayOutSplTokenV1InstructionData>([['discriminator', u8()]], {
      description: 'PayOutSplTokenV1InstructionData',
    }),
    (value) => ({ ...value, discriminator: 6 })
  ) as Serializer<
    PayOutSplTokenV1InstructionDataArgs,
    PayOutSplTokenV1InstructionData
  >;
}

// Instruction.
export function payOutSplTokenV1(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: PayOutSplTokenV1InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglGamePot',
    'BHPoT3HQSWiPD8AczqvCbudyicPmE8RN8bpcyZLzMGmb'
  );

  // Accounts.
  const resolvedAccounts = {
    pot: { index: 0, isWritable: true as boolean, value: input.pot ?? null },
    gameAuthority: {
      index: 1,
      isWritable: true as boolean,
      value: input.gameAuthority ?? null,
    },
    winner: {
      index: 2,
      isWritable: false as boolean,
      value: input.winner ?? null,
    },
    tokenMint: {
      index: 3,
      isWritable: false as boolean,
      value: input.tokenMint ?? null,
    },
    potTokenAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.potTokenAccount ?? null,
    },
    winnerTokenAccount: {
      index: 5,
      isWritable: true as boolean,
      value: input.winnerTokenAccount ?? null,
    },
    gameAuthorityTokenAccount: {
      index: 6,
      isWritable: true as boolean,
      value: input.gameAuthorityTokenAccount ?? null,
    },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    associatedTokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    systemProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.pot.value) {
    resolvedAccounts.pot.value = findGamePotPda(context, {
      gameAuthority: expectPublicKey(resolvedAccounts.gameAuthority.value),
      tokenMint: expectPublicKey(resolvedAccounts.tokenMint.value),
    });
  }
  if (!resolvedAccounts.potTokenAccount.value) {
    resolvedAccounts.potTokenAccount.value = findAssociatedTokenPda(context, {
      mint: expectPublicKey(resolvedAccounts.tokenMint.value),
      owner: expectPublicKey(resolvedAccounts.pot.value),
    });
  }
  if (!resolvedAccounts.winnerTokenAccount.value) {
    resolvedAccounts.winnerTokenAccount.value = findAssociatedTokenPda(
      context,
      {
        mint: expectPublicKey(resolvedAccounts.tokenMint.value),
        owner: expectPublicKey(resolvedAccounts.winner.value),
      }
    );
  }
  if (!resolvedAccounts.gameAuthorityTokenAccount.value) {
    resolvedAccounts.gameAuthorityTokenAccount.value = findAssociatedTokenPda(
      context,
      {
        mint: expectPublicKey(resolvedAccounts.tokenMint.value),
        owner: expectPublicKey(resolvedAccounts.gameAuthority.value),
      }
    );
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.associatedTokenProgram.value) {
    resolvedAccounts.associatedTokenProgram.value = publicKey(
      'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
    );
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getPayOutSplTokenV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
