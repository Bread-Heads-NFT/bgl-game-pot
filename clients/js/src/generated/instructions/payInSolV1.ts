/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type PayInSolV1InstructionAccounts = {
  /** The address of the game pot */
  pot: PublicKey | Pda;
  /** The participant */
  participant: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type PayInSolV1InstructionData = { discriminator: number };

export type PayInSolV1InstructionDataArgs = {};

export function getPayInSolV1InstructionDataSerializer(): Serializer<
  PayInSolV1InstructionDataArgs,
  PayInSolV1InstructionData
> {
  return mapSerializer<
    PayInSolV1InstructionDataArgs,
    any,
    PayInSolV1InstructionData
  >(
    struct<PayInSolV1InstructionData>([['discriminator', u8()]], {
      description: 'PayInSolV1InstructionData',
    }),
    (value) => ({ ...value, discriminator: 3 })
  ) as Serializer<PayInSolV1InstructionDataArgs, PayInSolV1InstructionData>;
}

// Instruction.
export function payInSolV1(
  context: Pick<Context, 'programs'>,
  input: PayInSolV1InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglGamePot',
    'BHPoT3HQSWiPD8AczqvCbudyicPmE8RN8bpcyZLzMGmb'
  );

  // Accounts.
  const resolvedAccounts = {
    pot: { index: 0, isWritable: true as boolean, value: input.pot ?? null },
    participant: {
      index: 1,
      isWritable: true as boolean,
      value: input.participant ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getPayInSolV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
